{
  "name": "noteplan-mcp",
  "version": "1.0.0",
  "description": "MCP server for NotePlan note and task management",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "npm run build:swift && tsc",
    "build:swift": "swiftc -o scripts/calendar-helper scripts/calendar-helper.swift && swiftc -o scripts/reminders-helper scripts/reminders-helper.swift",
    "build:ts": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "better-sqlite3": "^11.0.0",
    "uuid": "^9.0.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.11",
    "@types/node": "^20.0.0",
    "@types/uuid": "^9.0.0",
    "typescript": "^5.0.0"
  }
}
